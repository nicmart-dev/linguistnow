{
  "name": "linguistnow",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel server client",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "server-dev": "cd server && npm run dev",
    "test": "npm-run-all --parallel server client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
