# Server Configuration
# Port for the Express server to listen on
PORT=8080
# Base URL for the backend API
BACKEND_URL=http://localhost:8080
# Frontend URL (used for OAuth redirect URI - must match frontend VITE_BASE_URL)
FRONTEND_URL=http://localhost:3000

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# See: docs/set-up-oauth-in-google-cloud.md
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# OAuth redirect URI (must match the redirect_uri used in frontend Login.jsx)
# This should be the frontend URL, not the backend URL
GOOGLE_REDIRECT_URI=${FRONTEND_URL}

# Airtable Database Configuration
# Personal Access Token: Create at https://airtable.com/create/tokens
# Token starts with "pat" (e.g., patxxxxxxxxxxxxx)
AIRTABLE_PERSONAL_ACCESS_TOKEN=your_personal_access_token_here
# Base ID: Found in your Airtable base URL (starts with "app")
# Example: https://airtable.com/appXXXXXXXXXXXXXX/... -> appXXXXXXXXXXXXXX
# See: https://support.airtable.com/docs/finding-airtable-ids
AIRTABLE_BASE_ID=your_airtable_base_id_here

# N8N Workflow Automation Configuration
# Base URL for your n8n instance
# For local n8n: http://localhost:5678
# For remote n8n (HTTPS): https://your-n8n-instance.com
N8N_BASE_URL=http://localhost:5678
# Optional: Custom webhook path (defaults to /webhook/calendar-check)
# Only set this if you've customized the webhook path in your n8n workflow
# N8N_WEBHOOK_PATH=/webhook/calendar-check
